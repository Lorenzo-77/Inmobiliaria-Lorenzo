@model InmobiliariaLorenzo.Models.EmailRecuperoView

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <title>Recuperación de contraseña</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
</head>
<body style="background-color: #f8f9fa;">
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow-sm border-0">
                    <div class="card-header bg-primary text-white text-center">
                        <h4 class="mb-0"><i class="bi bi-shield-lock-fill"></i> Recuperación de Contraseña</h4>
                    </div>
                    <div class="card-body">
                        <p>¡Hola <strong>@Model.Nombre</strong>!</p>
                        <p>Hemos recibido una solicitud para restablecer tu contraseña. Para continuar, hacé clic en el siguiente botón:</p>

                        <div class="text-center my-4">
                            <a href="@(Model.Enlace)?access_token=@Model.Token" class="btn btn-success btn-lg">
                                <i class="bi bi-arrow-repeat"></i> Restablecer Contraseña
                            </a>
                        </div>

                        <p class="text-muted small">
                            Si no solicitaste este cambio, podés ignorar este mensaje. Tu contraseña seguirá siendo la misma.
                        </p>
                    </div>
                    <div class="card-footer text-center text-muted small">
                        © @DateTime.Now.Year - Inmobiliaria Lorenzo
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
